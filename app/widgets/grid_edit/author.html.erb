<table class="edit_table">
	<tr>
		<td>Name</td>
		<td><%= text_field_tag "#{container}[name]", record.name %></td>
	</tr>	
</table>
<% content_for :after_form do %>
	<div style="border:black solid thin;padding:3px;">
		<%= render_widget :book %>
	</div>
<% end %>