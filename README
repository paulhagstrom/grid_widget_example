To make this go, you should be able to

git submodule init
git submodule update
(to pull in the grid_edit plugin, which is a submodule)

Nothing very fancy at the moment.  I will try to add more examples (filters, toggles, etc.)
